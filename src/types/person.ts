export interface IPersonResponse {
  ident: string,
  navn: string,
  fornavn: string,
  mellomnavn: string,
  etternavn: string,
  foedselsdato: string,
  aktoerId: string
}

export interface IPerson {
  navn: string,
  fodselsnummer: string
}